#include "stdafx.h"

using namespace std;

class Solution1606BusiestServers {
public:
    vector<int> busiestServers(int k, vector<int>& arrival, vector<int>& load) {        
        vector<int> servers(k);
        
    }
};
